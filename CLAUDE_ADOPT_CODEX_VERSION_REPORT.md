# 給 Claude Code 的採用報告：請改用 Codex 完成版

## 一句話結論

請採用 `C:\Users\asd81\Documents\CalumAi\GAME2改版` 這一份 Codex 修改後版本，作為接下來的主版本。不要再以 `C:\Users\asd81\Documents\Claude\04-game2` 作為修改基準，也不要覆蓋原始資料夾。

## 專案背景

這是一個以台灣原住民族文化、家屋建築、卡牌蒐集、玩家互動與 STEAM 教育為核心的數位桌遊網站。使用者希望網站看起來更像可商業販售的遊戲，而不是普通網頁教材；同時要兼具互動性、教育深度與課堂使用價值。

原始資料夾：

```text
C:\Users\asd81\Documents\Claude\04-game2
```

Codex 修改後主版本：

```text
C:\Users\asd81\Documents\CalumAi\GAME2改版
```

後續請以 `GAME2改版` 為主，不要直接改原始 `04-game2`。

## 為什麼要採用 Codex 版本

Codex 版本已經完成下列方向：

1. 遊戲網站的整體 UI 已經改成更像遊戲主機級作品的桌遊介面。
2. 回合事件變多，遊戲節奏比較有變化。
3. 玩家之間新增正向互動，不只剩下競爭與偷襲。
4. 骰子動畫已重做，避免重複擲骰與動畫斷裂。
5. 抽卡操作已放到中央檯面，降低一直開行動選單的麻煩。
6. 不能抽卡時會直接顯示原因，例如行動點數不足、牌庫已空。
7. 背景音樂已移除，預設聲音為 OFF，避免干擾。
8. 新增 STEAM 教育頁，可依五個年級程度產生教案。
9. 已建立測試與驗證流程，方便後續 AI 或人類接手。

## 最新一次修改重點

### 1. 抽卡牌庫常駐中央檯面

檔案：

```text
app.js
remaster.css
```

修改內容：

- 原料牌庫與文化牌庫已放在中央檯面。
- 玩家不需要一直打開行動選單才能抽卡。
- 牌庫可抽時會直接執行抽卡。
- 牌庫不可抽時仍可點擊，並顯示明確原因。

驗收方式：

- 開局後擲骰。
- 點中央檯面的原料牌庫。
- 應看到牌庫張數減少、行動點扣除、手牌新增。
- 行動點不足時點牌庫，應提示「行動點數不足」。

### 2. 抽卡防呆集中處理

檔案：

```text
app.js
```

重點函式：

```text
drawBlockedReason()
actionDrawMaterial()
actionDrawCulture()
```

修改目的：

- 不讓不同 UI 入口各自判斷抽卡條件。
- 讓中央牌庫、空手牌狀態、行動選單、未來快捷鍵都使用同一套判斷。

### 3. 移除背景音樂

檔案：

```text
audio.js
```

修改內容：

- `Sound.scene()` 保留，但不再播放循環配樂。
- `Sound.startAmbient()` 保留，但不再啟動背景音樂。
- 第一次點擊頁面只解鎖短音效，不會自動開始配樂。
- 聲音預設為 OFF。

驗收方式：

- 重新整理頁面。
- 右下角聲音狀態應顯示 `OFF / 聲音已關閉`。
- 進入遊戲後不應自動播放背景音樂。

## Codex 版本已包含的主要功能

1. 回合事件從 6 個擴充到 14 個。
2. 新增玩家互助行動：
   - 互助分享素材
   - 共學交流
   - 互助分數
3. Bot 會使用互助行動。
4. 骰子動畫改為階段式狀態：
   - ready
   - launch
   - rolling
   - result
5. 新增 STEAM 教育頁。
6. 教案分成五個程度：
   - 國小 1-2 年級
   - 國小 3-4 年級
   - 國小 5-6 年級
   - 國中
   - 高中
7. 教案主題可連結：
   - 卡牌
   - 建築
   - 台灣原住民族文化
   - 台灣氣候
   - STEAM 探究任務

## 請 Claude Code 接手時遵守

### 必須做

1. 以 `C:\Users\asd81\Documents\CalumAi\GAME2改版` 作為主版本。
2. 修改前先讀：
   - `AGENTS.md`
   - `ARCHITECTURE.md`
   - `TESTING.md`
   - `CHANGELOG.md`
   - `docs/01_PROJECT_BRIEF.md`
   - `docs/08_STEAM_EDUCATION.md`
3. 修改後一定要跑測試：

```powershell
cd "C:\Users\asd81\Documents\CalumAi\GAME2改版"
npm.cmd test
```

4. 若要啟動預覽：

```powershell
cd "C:\Users\asd81\Documents\CalumAi\GAME2改版"
npm.cmd start
```

5. 若 3000 或 3001 被佔用，`serve.js` 會自動嘗試下一個 port。

### 不可以做

1. 不要覆蓋 `C:\Users\asd81\Documents\Claude\04-game2`。
2. 不要把 Codex 版本回退成舊版 UI。
3. 不要重新打開背景音樂，除非使用者明確要求。
4. 不要把抽卡操作又藏回行動選單。
5. 不要移除抽卡失敗原因提示。
6. 不要在沒有文化顧問審查前，把族群文化內容宣稱為正式商用定稿。

## 目前已驗證結果

Codex 已執行：

```powershell
npm.cmd test
```

結果：

- 語法檢查通過。
- STEAM 教案測試 500 份通過。
- 事件與互助機制測試通過。
- 100 場模擬全數完賽。
- 平均輪數約 12.5。
- Browser console 無 error / warning。

瀏覽器實測：

- 中央檯面可看到原料牌庫與文化牌庫。
- 擲骰後點原料牌庫可成功抽卡。
- 抽卡後行動點會扣除。
- 抽卡後手牌會增加。
- 行動點不足時，牌庫會顯示不能抽的原因。
- 聲音預設為 OFF。

## 給 Claude Code 的下一步工作單

背景：

使用者要採用 Codex 完成的 `GAME2改版` 版本，並以此作為後續主版本。

目標：

確認並延續 Codex 版本，不要回退、不覆蓋原始資料夾。

修改範圍：

```text
C:\Users\asd81\Documents\CalumAi\GAME2改版
```

不可修改範圍：

```text
C:\Users\asd81\Documents\Claude\04-game2
```

完成條件：

1. 能啟動網站。
2. 中央檯面有可點擊抽卡牌庫。
3. 不能抽卡時會顯示原因。
4. 背景音樂不會自動播放。
5. STEAM 教育頁仍可使用。
6. `npm.cmd test` 通過。

輸出格式：

請回報：

- 是否已確認採用 Codex 版本。
- 是否有修改任何檔案。
- 測試結果。
- 若有問題，列出問題、原因、修正方式。

## 最後提醒

這份版本可以作為展示與後續開發主線，但若要正式商業販售，仍需要補：

1. 原住民族文化顧問審查。
2. 圖像、音效、文字素材授權清單。
3. 教案列印版與教師使用測試。
4. 商用版本的品牌、商標與上架規格。
