# Claude 回來接手筆記

## 這次 Codex 接手修改的方向

使用者說 Claude token 用完，所以 Codex 先接手修「剛剛說要修正的地方」。這次不做大重構，直接處理目前影響遊戲主線的核心問題：

- 建築勝利觸發不一致。
- 失敗行動可能吃掉行動點。
- 工藝池太早讓遊戲結束，導致「蓋家屋」主目標不明顯。
- Bot 行為偏向工藝與文化加分，沒有優先走建築勝利路線。
- 玩家主畫面不知道下一步該做什麼。

## 已修改檔案

- `game-engine/actions.js`
- `game-engine/bot.js`
- `game-engine/simulate.js`
- `app.js`
- `style.css`
- `CODEX_CLAUDE_HANDOFF.md`

## 核心規則修改

### 1. 建築勝利檢查共用化

新增 `checkBuildingSetWin(state, p)`。

現在以下路徑取得建築後，都會檢查是否集滿本族 4 間家屋：

- `BUY_BUILDING`
- 文化卡效果 `draw_building`
- `SWAP_BUILDING` 猜拳成功取得建築

目的：

避免玩家不是透過「買建築」取得第 4 間本族家屋時，勝利條件沒有被觸發。

### 2. 失敗行動不應扣行動點

調整多個 action 的驗證順序。

原則改成：

1. 先確認行動點足夠。
2. 再確認目標、素材、手牌、牌池是否有效。
3. 確認全部可執行後才扣行動點。

已處理的行動包含：

- `RAID`
- `TRADE`
- `DRAW_MATERIAL_CARD`
- `DRAW_CULTURE_CARD`
- `PLAY_RAW_PAIR`
- `PLAY_CULTURE`
- `BUY_BUILDING`
- `BUY_FROM_PLAYER`
- `SWAP_BUILDING`
- `FORCE_SWAP_RAW`

另外補了一個小洞：

- `PLAY_RAW_PAIR` 現在會檢查 `craftId` 是否存在。
- 工藝池沒有該工藝時，不會被當成成功行動。

### 3. 工藝池抽光不再直接結束遊戲

原本 `takeCraft()` 會在 `craftPool.length === 0` 時設：

```js
state.endTriggeredBy = 'craft_pool_empty'
```

這會讓遊戲大量以工藝池耗盡收尾，削弱「蓋家屋」作為主目標的地位。

現在已移除此結束條件。

新的設計假設：

- 工藝池是加分路線。
- 建築才是主勝利路線。
- 工藝池空了，遊戲仍可繼續蓋家屋。

### 4. 新增建築牌庫抽光結束條件

新增結束原因：

```js
building_deck_empty
```

目前在兩個地方補上：

- `app.js` 的回合推進流程。
- `game-engine/simulate.js` 的模擬流程。

意義：

如果建築牌庫已經抽光，而且沒有人剛好觸發本族建築集滿，遊戲會進入收尾結算。

這比「工藝池抽光就結束」更符合目前 UI 呈現的主目標：蓋滿 4 間家屋。

### 5. Bot 優先走建築路線

`game-engine/bot.js` 已調整優先序。

原本 Bot 順序大致是：

1. 原料配對換工藝。
2. 打文化卡。
3. 素材足夠才買建築。

現在改成：

1. 如果有 4 種素材且有 2 點行動，先買建築。
2. 再考慮換工藝。
3. 再考慮文化卡與其他行動。

目的：

讓 Bot 的行為符合遊戲主線，不要一直追加分而延後家屋勝利。

## UI / 體驗修改

### 1. 新增回合提示

`app.js` 新增 `turnHint(p)`。

行動面板會顯示一行提示，例如：

- 行動點用完了，請結束回合。
- 四種素材到齊了，現在可以優先蓋家屋。
- 你手上有可配對原料，可以換工藝。
- 先補素材吧，目前缺某些素材。

目的：

降低新玩家不知道下一步要做什麼的問題。

### 2. 可用行動高亮

`app.js` 新增 `actionButton(...)` 統一生成行動按鈕。

`style.css` 新增 `.action-ready` 樣式。

目前可執行的行動會有左側亮線與小光點。

目的：

玩家不用讀完所有按鈕，就能快速看出哪些可以按。

## Claude 建議回來後優先確認

### A. 結束條件是否符合你的正式設計

目前 Codex 先採用：

- 建築集滿本族 4 間：觸發收尾。
- 建築牌庫抽光：觸發收尾。
- 三個牌庫都空且無人可再換工藝：觸發收尾。
- 工藝池抽光：不再觸發收尾。

如果正式規則仍希望工藝池抽光就結束，請再改回。但從目前遊戲主目標來看，建議不要讓工藝池成為主要結束條件。

### B. Bot 是否還需要更聰明

目前 Bot 只是簡單調整優先序，還不是完整策略 AI。

後續可再補：

- 快集滿家屋時，優先搶本族建築。
- 缺素材時，優先拿缺的素材。
- 對手快贏時，優先偷襲或建築互換。

### C. UI 是否要繼續收斂

這次只加提示與高亮，沒有大改版面。

後續仍建議處理：

- 進階行動收合。
- 手機版行動區高度。
- 禁用按鈕原因在手機上的呈現。
- 擲骰彈窗節奏。

## 使用者要求

使用者明確說：

> 不用驗證啦，你直接先改 claude 要改的地方，改完有個筆記給他看就好

所以這一輪沒有做完整驗證流程。Claude 回來後若要正式合併，建議至少跑：

```bash
node --check app.js
node --check game-engine/actions.js
node game-engine/simulate.js 1000
```

